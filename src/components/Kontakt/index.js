import React, { Component, PropTypes } from 'react'
import styles from './styles.css'

class Kontakt extends Component {
  render() {
    return (
      <section styleName="kontakt">Kontakt</section>
    )
  }
}

export default Kontakt
