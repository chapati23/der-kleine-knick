import React, { Component, PropTypes } from 'react'
import styles from './styles.css'

class Shop extends Component {
  render() {
    return (
      <section styleName="shop">Shop</section>
    )
  }
}

export default Shop
